import React from "react";
const ProfileOverview = () => {
    return (
        <h1 className="resources">You have not Listed Any resources, Click the + icon to begin</h1>
    );
}

export default ProfileOverview;